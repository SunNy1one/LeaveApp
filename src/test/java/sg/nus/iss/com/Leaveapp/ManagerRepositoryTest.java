package sg.nus.iss.com.Leaveapp;

import java.util.List;

import org.junit.jupiter.api.Test;

import sg.nus.iss.com.Leaveapp.model.Leave;
import sg.nus.iss.com.Leaveapp.repository.LeaveRepository;

public class ManagerRepositoryTest {
	
//	 @Test
//	    public void testGetEmployeeLeaveHistory() {
//	        when(LeaveRepository.findByEmployeeOrderByStartDesc(employee)).thenReturn(Arrays.asList(leave));
//
//	        List<Leave> result = managerService.getEmployeeLeaveHistory(employee);
//
//	        assertNotNull(result);
//	        assertEquals(1, result.size());
//	        assertEquals(leave.getId(), result.get(0).getId());
//	    }

}
